export interface TimeFormat {
    hours: string;
    minutes: string;
    seconds: string;
}

export interface Timezone {
    timezone: string;
    offset: number;
    region: string;
}

export interface Vector2D {
    x: number;
    y: number;
}